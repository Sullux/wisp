; This is a placeholder test file for the Wisp standard library.

(describe 'Wisp Standard Library'
  (lambda ()
    (it 'should have tests'
      (lambda ()
        (. (expect true) (toBe true))))))

